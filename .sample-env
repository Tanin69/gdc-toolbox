# .env
PORT=3000
APP_NAME=gdc-server

#
# Be careful : node fs module used. On Windows Server, you should use unix like name path, without any escape characters
# Don't forget to include the last slash
# ex. 
# TMP_DIR="c:/gdc-server/tmp/"
#

# Path to the mikero tool 
DEPBO_EXE_PATH=""
# A directory where pbo are dpo
TMP_DIR=""
# Directory where users uploads their pbo.
INPUT_DIR=""
# Directory where briefing and images are saved (! not implemented at the moment, but mandatory !)
OUTPUT_DIR=""
# Arma 3 missions directory
MISSIONS_DIR=""